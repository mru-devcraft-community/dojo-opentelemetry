# Solution — Étape 00

Aucune modification de code n'est nécessaire pour cette étape.

Le projet de base (`java/src/`) fonctionne tel quel. Cette étape consiste uniquement à :

1. Vérifier l'environnement (Java 21, Maven, Docker)
2. Lancer l'infrastructure avec `docker compose up -d`
3. Builder et lancer l'application
4. Tester les endpoints REST

Le code source reste inchangé.
