<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Appender console avec TraceId et SpanId dans le pattern -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [trace_id=%mdc{trace_id}] [span_id=%mdc{span_id}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender OpenTelemetry — exporte les logs vers le Collector OTLP -->
    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureKeyValuePairAttributes>true</captureKeyValuePairAttributes>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OTEL"/>
    </root>
</configuration>
